# API Backend Requirements
# Senior Dev: Backend dependencies for FastAPI server

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
google-generativeai>=0.3.0

# Note: Other dependencies (jugaad-data, nselib) already in main requirements.txt
